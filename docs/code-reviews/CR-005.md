# CR-005 — React Dashboard Implementation

**Status:** approved
**Created:** 2026-06-14
**Reviewer:** Claude (Automated Review)
**Traces:** REQ-005
**Covers:** ARCH-005, TEST-005

---

## Summary

Code review for REQ-005: the `frontend/` Vite + React + TypeScript SPA
consuming the read-only REST API from REQ-003/REQ-004 (`/picnic/api/...`),
implementing the Home, Stats, and Receipts pages per ARCH-005.

**Scope:**
- Project scaffold: `frontend/package.json`, `vite.config.ts` (dev proxy to
  `/picnic` on `http://localhost:8000`), `tsconfig*.json`,
  `tailwind.config.js`, `postcss.config.js`, `eslint.config.js`,
  `.prettierrc.json`, `index.html`
- `src/api/client.ts` — `fetchJson<T>()` wrapper over `fetch`, base path
  `/picnic/api`
- `src/types/index.ts` — TS interfaces mirroring `backend/schemas.py`
- `src/hooks/useStats.ts`, `useReceipts.ts`, `useProducts.ts` — TanStack
  Query hooks, one per endpoint
- `src/store/useUiStore.ts` — Zustand store for UI-only state (stats period,
  price-history range/product, receipts pagination offset)
- `src/lib/format.ts` — `formatCents`, `getCurrentMonth`, `rangeToFromDate`
- `src/components/common/{LoadingSpinner,ErrorMessage,EmptyState}.tsx`
- `src/components/Dashboard.tsx` (AC-005-01), `Budget/BudgetWidget.tsx`
  (AC-005-05), `Charts/PurchaseStats.tsx` (AC-005-03),
  `Charts/PriceHistory.tsx` (AC-005-02), `Receipts/ReceiptList.tsx` +
  `Receipts/ReceiptDetail.tsx` (AC-005-04)
- `src/pages/{Home,Stats,Receipts}.tsx`, `src/App.tsx` (React Router nav +
  routes), `src/main.tsx` (QueryClientProvider + BrowserRouter)
- `frontend/tests/{Dashboard,Charts,Receipts,Budget}.test.tsx` +
  `tests/test-utils.tsx`

**Test Results:**
- 14/14 Vitest tests passing (TC-005-01 .. TC-005-06)
- `tsc -b` (typecheck/build) passes; `vite build` succeeds
- `eslint .` and `prettier --check .` pass
- Manual verification: backend (`uvicorn`) + frontend (`vite dev`) started
  together; `/picnic/api/*` endpoints reachable through the Vite dev proxy
  (`stats/summary`, `stats/budget`, `receipts` all returned expected JSON)

---

## Checklist

### Traceability

- ✅ REQ-005 exists and is complete (6 ACs)
- ✅ ARCH-005 exists and traces to REQ-005, resolving all three pending
  questions (routing, dev proxy, theme)
- ✅ TEST-005 exists and covers all 6 ACs (6 test cases, several with
  multiple `it()` blocks)
- ✅ All test cases reference their TC-ID in a comment above the `it()`
  block
- ✅ Implementation commit references REQ-005

### Code Quality

- ✅ Follows coding style (KISS, DRY, YAGNI, early returns for
  loading/error/empty states)
- ✅ No `any` types; `strict` TS enabled (`noUnusedLocals`,
  `noUnusedParameters`)
- ✅ No magic numbers — `PAGE_SIZE`, `RANGES`, `PERIODS`, `RANGE_MONTHS` are
  named constants
- ✅ No commented-out code
- ✅ Components are single-responsibility: one hook per endpoint, one
  component per AC, shared `common/` components for loading/error/empty
- ✅ Early returns used instead of deep nesting (`Dashboard`, `BudgetWidget`,
  `ReceiptList`, `ReceiptDetail`)
- ✅ `eslint .` and `prettier --check .` pass with zero warnings

### Tests

- ✅ TEST-005 created with one TC per AC before component implementation
- ✅ All ACs have test cases:
  - AC-005-01: TC-005-01 (loading, success, error states)
  - AC-005-02: TC-005-02 (chart + min/max/avg, configurable range)
  - AC-005-03: TC-005-03 (top items + spending chart, period switch)
  - AC-005-04: TC-005-04 (paginated list sorted desc, receipt detail)
  - AC-005-05: TC-005-05 (under-budget vs. over-budget styling)
  - AC-005-06: TC-005-06 (retry on error, zero-value render without crash)
- ✅ Tests follow AAA structure
- ✅ No real I/O — `fetchJson` is mocked via `vi.spyOn`; Zustand store reset
  in `beforeEach` for test independence
- ✅ Edge cases covered: loading spinners, error + retry, empty datasets
  (no spending buckets, no top items, no price-history points, no receipts),
  zero-value summary stats

### Security

- ✅ No secrets or credentials in code/comments
- ✅ `fetchJson` only calls relative, same-origin `/picnic/api/...` paths —
  no user-controlled URLs
- ✅ No `dangerouslySetInnerHTML` or other XSS-prone constructs; all data
  rendered via JSX text interpolation
- ✅ Read-only UI — no mutating requests
- ✅ New dependencies (`react-router-dom`, `@tanstack/react-query`,
  `zustand`, `recharts`, Tailwind/ESLint/Vitest tooling) are all already
  named in CLAUDE.md's frontend stack table

### Documentation

- ✅ ARCH-005 created, resolving all "Questions / Decisions Pending" from
  REQ-005 (routing, dev proxy, theme conventions)
- ✅ TEST-005 created and cross-referenced
- ✅ REQ-005 `Traced by` field updated to `ARCH-005, TEST-005`
- ✅ No breaking changes to the backend (REQ-001..004)

---

## Findings

No blockers or majors found.

| # | Severity | File / Line | Description |
|---|----------|-------------|--------------|
| 1 | idea | `frontend/src/pages/Receipts.tsx` | The `/receipts` and `/receipts/:id` routes are both handled by the same `Receipts` page component, switching on `useParams().id`. This keeps routing simple per ARCH-005, but if a dedicated receipt-detail layout (e.g. a "back to list" link) is needed later, splitting into two route elements would be a small follow-up. |
| 2 | idea | `frontend/src/components/Charts/PriceHistory.tsx` | `rangeToFromDate("all")` returns `undefined`, which `fetchJson` simply omits from the query string — relies on the backend treating a missing `from_date` as "no lower bound" (confirmed in `backend/api/routes.py`/`stats_service.get_price_trend`). |
| 3 | idea | `frontend/package.json` | `pages/Settings.tsx` from CLAUDE.md's structure is intentionally not created — ARCH-005 documents this as deferred until a concrete Phase 2 requirement (IMAP config UI) exists. |

These are not bugs — noted for future iterations.

---

## Decision

- ✅ **Approved** — ready to merge

### Reasoning

The implementation satisfies all acceptance criteria of REQ-005:
- ✅ AC-005-01: Home page renders `/api/stats/summary` headline figures with
  loading and error states
- ✅ AC-005-02: `PriceHistory` renders a Recharts line chart with a product
  selector and a configurable 3m/6m/12m/all range
- ✅ AC-005-03: `PurchaseStats` visualizes top items and spending-over-time,
  with a week/month aggregation toggle
- ✅ AC-005-04: `ReceiptList`/`ReceiptDetail` show paginated receipts
  (newest first, as returned by the API) and full line-item detail
- ✅ AC-005-05: `BudgetWidget` shows spend vs. budget for the current month,
  with a visually distinct red over-budget state vs. green under-budget
- ✅ AC-005-06: All data-bound components use TanStack Query with consistent
  loading/error/empty handling via shared `common/` components; error states
  offer a retry action

**Code quality:**
- Follows all conventions in `.claude/rules/`
- 14/14 tests passing; `tsc -b`, `vite build`, `eslint`, and
  `prettier --check` all pass
- No security issues; no unapproved new dependencies

### Next Steps

1. **Merge to main** — this implementation is complete for Phase 1 MVP
2. **Phase 2 follow-up**: `pages/Settings.tsx` / IMAP config UI, when a
   concrete requirement (REQ-XXX) is written for it

---

*Approved by: Claude Code (Automated Review)*
*Date: 2026-06-14*
